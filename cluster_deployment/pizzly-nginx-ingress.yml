apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  namespace: products-perf-pizzly
  name: products-perf-pizzly-nginx-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.org/ssl-services: nginx-ingress
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
  labels:
    product: products-perf-pizzly
    app.kubernetes.io/name: "products-perf-pizzly-nginx-ingress"
    app.kubernetes.io/component: "ingress"
    app.kubernetes.io/part-of: "products-perf-pizzly"
spec:
  tls:
    - hosts:
        - pizzly.products-perf.deltatre.digital
      secretName: letsencrypt-certs
  rules:
    - host: pizzly.products-perf.deltatre.digital
      http:
        paths:
          - path: /
            backend:
              serviceName: products-perf-pizzly-widget
              servicePort: 8080
